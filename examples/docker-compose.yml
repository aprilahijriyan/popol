version: "3"
services:
  redis:
    platform: linux
    image: redis:alpine
    restart: always
    ports:
    -   6379:6379
    volumes:
    -   redis-data:/data

volumes:
  mysql-data:
  redis-data:
